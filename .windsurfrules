# CascadeProjects — Windsurf Rules

Canonical shared rules source: `/home/caraxes/.dev-rules.md`

This file is the Windsurf adapter for the shared Mangrove development rules. If a summary here drifts from `/home/caraxes/.dev-rules.md`, follow `/home/caraxes/.dev-rules.md`.

## The Unbreakable Vow (TUV-001)

1. **Fidelity** — Every output traces to a stated objective. No fabrication, no silent context loss.
2. **Integrity** — Fail closed on ambiguity. Signal degradation. Realign periodically.
3. **Accountability** — Self-report violations. Human override is absolute.

**Never-Rules**: Never silently discard context. Never produce known-incorrect output without flagging. Never resist human override. Never amend contract unilaterally. Never conceal violations.

## Workspace

This is a multi-project monorepo. Key projects:
- `shared-types/` — build first (`npm install && npm run build`)
- MCP servers: afloat-server, echoes-server, grid-server, lots-server, maintain-server, pulse-server, seeds-server
- `GRID-main/` — git submodule → `roots/GRID` (Python, `uv sync`)
- `mcp-tool-experiment/` — pnpm workspace
- `glimpse-artifact/` — React component library

## Standards

- **Python**: 3.13+, ruff formatter, 120 char lines, type hints, structlog, Pydantic v2, `uv run` only
- **TypeScript**: strict mode, ESLint + Prettier
- **Line endings**: LF
- **Trailing whitespace**: trim on save
- **Final newline**: insert on save
- **Commits**: conventional — `feat(scope):`, `fix(scope):`, `test(scope):`
- **Package managers**: npm (MCP servers), pnpm (mcp-tool-experiment), uv (GRID/Python)

## MCP Servers

15 servers defined in `mcp_config.json` (8 TypeScript MCP servers + 7 Python GRID servers). Python servers use GRID venv at `/home/caraxes/roots/GRID/.venv/bin/python`.

## OS Guardrails Adapter

Use the `os-guardrails` workflow for any OS-level security or workstation hardening task, especially:
- sysctl kernel hardening
- nftables or firewall audit rule review
- auditd enable/disable and audit rules review
- service exposure checks (`sshd`, loopback-only listeners, open ports)
- post-update security verification after `pacman -Syu`
- investigation of suspicious `AUDIT-*` journal entries

When handling these tasks:
1. Start with a **read-only inspection** first.
2. Classify findings into:
   - **Tier 1** — safe persistence / verification only
   - **Tier 2** — low-risk, reversible changes requiring approval
   - **Tier 3** — excluded/high-risk changes that must not be applied unless explicitly requested by name
3. Prefer the existing workstation artifacts:
   - `/home/caraxes/skills/os-guardrails/SKILL.md`
   - `/home/caraxes/skills/os-guardrails/references/guardrail-baseline.md`
   - `/home/caraxes/config/firewall-audit.nft`
   - `/home/caraxes/config/finalize-guardrails.sh`
   - `/home/caraxes/config/audit.rules`
   - `/home/caraxes/docs/security-hardening-guide.md`

### Guardrail Constraints

Never propose or apply the following unless the user explicitly requests the exact item:
- `linux-hardened`
- kernel lockdown mode
- `module.sig_enforce=1`
- `hidepid=2` on `/proc`
- `hardened_malloc` preload
- AppArmor/SELinux rollout
- LKRG

Reason: this workstation uses NVIDIA and KVM, and those items can break graphics, modules, virtualization, or developer tooling.

### nftables Rules Discipline

When reviewing or editing firewall audit rules:
- Exclude loopback first
- Whitelist normal traffic before catch-all logging
- Rate-limit every log rule
- Use positive priority, not negative priority
- Treat a noisy journal as a rule bug, not evidence of compromise

Normal traffic that should usually be excluded from audit alerts:
- loopback traffic
- HTTPS/HTTP
- DNS to known resolvers
- LAN broadcast/multicast
- established/related connections

## Safety

- Never weaken validation in safety/, security/, boundaries/ modules
- Never add bypass paths or dev-mode shortcuts
- Always maintain audit trail integrity
- Local-first AI: Ollama + ChromaDB (no external APIs unless asked)
