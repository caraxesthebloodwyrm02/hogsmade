{
  "name": "pulse-server",
  "version": "1.0.0",
  "description": "Developer dashboard MCP server — morning briefings, cross-server status aggregation, session journals, and focus tracking",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc",
    "dev": "tsx --watch src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@cascade/shared-types": "file:../shared-types",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.6.0",
    "typescript": "^5.5.0",
    "vitest": "3.2.4"
  }
}
