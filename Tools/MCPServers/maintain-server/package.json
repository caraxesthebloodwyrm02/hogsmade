{
  "name": "maintain-server",
  "version": "1.0.0",
  "description": "System optimization & maintenance MCP server \u2014 scans temp/cache, workspaces, git repos, system health; executes cleanup with safety checks",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "dev": "tsx --watch src/server.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@cascade/shared-types": "file:../../../Components/shared-types",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "4.1.2",
    "tsx": "^4.6.0",
    "typescript": "~5.9.3",
    "vitest": "4.1.2"
  }
}
