{
  "name": "glass-server",
  "version": "1.0.0",
  "description": "Glass bridge emitter — MCP server for writing session state to the Glass field",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "dev": "tsx --watch src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cascade/shared-types": "file:../../../Components/shared-types",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.6.0",
    "typescript": "~6.0.2",
    "vitest": "4.1.4"
  }
}
