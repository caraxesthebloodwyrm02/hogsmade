{
  "name": "overview-server",
  "version": "1.0.0",
  "description": "Checkpoint-based situational awareness MCP server — trust instrument for the Mangrove workspace",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "dev": "tsx --watch src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@cascade/shared-types": "file:../../Components/shared-types",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.6.0",
    "typescript": "~5.9.3",
    "vitest": "3.2.4"
  }
}
