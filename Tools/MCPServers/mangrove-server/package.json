{
  "name": "mangrove-server",
  "version": "1.0.0",
  "description": "Dedicated MCP server for Mangrove DIO bridge and security audit tools",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "dev": "tsx --watch src/server.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@vitest/coverage-v8": "3.2.4",
    "tsx": "^4.6.0",
    "typescript": "~6.0.2",
    "vitest": "4.1.2"
  }
}
