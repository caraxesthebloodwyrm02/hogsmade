name: verify-tool-consolidation
on: [pull_request, push]
jobs:
  matrix-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check topic x tool matrix
        run: python3 Components/scripts/verify_tool_consolidation.py
